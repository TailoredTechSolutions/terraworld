import MemberSupportPanel from "@/components/member/MemberSupportPanel";

// Reuse member support panel for buyers
const BuyerSupportPanel = () => {
  return <MemberSupportPanel />;
};

export default BuyerSupportPanel;
